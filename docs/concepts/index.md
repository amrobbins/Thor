# Concepts

Concept pages explain the model behind Thor's Python API: tensors and batch semantics, networks and symbolic construction, execution, training, data flow, expressions, persistence, and runtime behavior.

These pages are intentionally separate from API reference material so that implementation concepts can be explained without turning reference pages into long tutorials.
