# Ensembles

Thor's ensemble artifact API is implemented in Python, so its public signatures and docstrings can be rendered directly from the package source without loading the native extension.

::: thor.ensembles.EnsembleAggregation

::: thor.ensembles.EnsembleMemberSpec

::: thor.ensembles.EnsembleModel
